<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/idRelativeLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/light_sage">


    <ImageButton
        android:id="@+id/history_imageBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true"
        android:layout_margin="30dp"
        android:background="@android:color/transparent"
        app:srcCompat="@drawable/ic_history_black" />

    <ImageButton
        android:id="@+id/add_note_imageBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_margin="30dp"
        android:layout_marginBottom="59dp"
        android:background="@android:color/transparent"
        app:srcCompat="@drawable/ic_note_add_black" />

    <ImageView
        android:id="@+id/smiley_happy_imageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_margin="50dp"
        android:contentDescription="@null"
        app:srcCompat="@drawable/smiley_happy" />

</RelativeLayout>
